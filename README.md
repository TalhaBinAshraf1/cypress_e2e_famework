# cypress_new_famework

New framework for Cypress e2e Testing

#Cypress Plugins

> > > File Upload

Usage ->
Now, let's see how we can actually test something. Exposed command has signature like:
cySubject.attachFile(fixture, optionalProcessingConfig);
